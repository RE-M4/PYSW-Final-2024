import { ApellidoMedicoPipe } from './apellido-medico.pipe';

describe('ApellidoMedicoPipe', () => {
  it('create an instance', () => {
    const pipe = new ApellidoMedicoPipe();
    expect(pipe).toBeTruthy();
  });
});
